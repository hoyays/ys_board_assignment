<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.example.demo.mapper.BoardMapper">

  <!-- 글쓰기 저장 -->
  <insert id="insertWrite">
  
  <!-- insert into hu_board(postNum, user_id, title, contents, boardDate, fileName, groupNum, indentNum, hitNum, stepNum) values(
 	board_seq.nextval, #{user_id}, #{title}, #{contents}, sysdate, #{fileName}, board_seq.currval, '0', '0', '0') -->
  
  
  

  </insert>
  
  
</mapper>
